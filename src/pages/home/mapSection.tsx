import { Col, Row } from 'antd';
import { MarkerClusterer } from "@googlemaps/markerclusterer";

// use default algorithm and renderer

const MapSction = () => {
    
    // const markerCluster = new MarkerClusterer({ map, markers });
    return ( 
        <Row>
            
            <h1>MapSection</h1>
        </Row>
     );
}
 
export default MapSction;